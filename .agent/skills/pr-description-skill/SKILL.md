---
name: PR description creation skills
description: Skill that manages the creation of PR descriptions
metadata:
  author: d1scw0rld
  version: "1.0"
---

* When you create a PR description use the differences between the current branch and the `master` branch
* Do not include code samples in the PR description
* The PR description should be concise and to the point, highlighting the main changes and their impact on the project
* Create a Markdown file with the PR description